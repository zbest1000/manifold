%% Demo-only ACL: allow every client everything, including $SYS/# and literal
%% "#" subscriptions (EMQX's shipped default denies both to non-local clients,
%% which would silently starve Manifold's full-namespace subscription and the
%% client-lifecycle $SYS feed). Never use this file outside a local demo.
{allow, all}.
